$schema: https://json-schema.org/draft/2020-12/schema
$id: https://shadowforgedmmo.com/schemas/characters/behaviors/set-velocity.schema.yaml
title: Set Velocity
description: Sets the character's velocity in local space
type: object
properties:
  type:
    type: string
    const: set_velocity
  velocity:
    description: Velocity vector in local space
    $ref: ../../vector3.schema.yaml
required:
  - type
  - velocity
additionalProperties: false